<%# https:// %>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= image_tag user.photo, alt: "picture of user", style: "width: 50px; height: 50px;" %>
      <%= link_to user.name, user_path(user) %>
      Number of posts: <%= user.posts_counter %>
    </li>
  <% end %>
</ul>